package com.adinsights.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SnowflakeConfig {
    // Spring Boot auto-configures DataSource from spring.datasource.*.
}